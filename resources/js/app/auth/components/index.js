import Vue from 'vue'

import login from './Login.vue'
import register from './Register.vue'

export const Login = Vue.component('login', login)
export const Register = Vue.component('register', register)